apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: cf-ws
  namespace: codefresh
spec:
  host: codefresh-dev.v12.np01.ocp.six-group.net
  path: /ws
  port:
    targetPort: 8080
  tls:
    termination: edge
  to:
    kind: Service
    name: cf-cfapi
    weight: 100
  # wildcardPolicy: None