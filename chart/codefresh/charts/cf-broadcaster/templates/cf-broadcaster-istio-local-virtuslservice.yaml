{{- if $.Values.global.istio.enabled }}
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ template "cf-broadcaster.fullname" $ }}
spec:
  hosts: 
  - {{ template "cf-broadcaster.fqdn" $ }}
  http:
{{- range $key,$value := .Values.global.istio.extraDeployments -}}
  {{- range $host, $apps := $value -}}
    {{- range $name, $a := $apps -}}
      {{- if (eq $name "cf-broadcaster") }}
  - match:
    - headers:
        x-codefresh-version:
          exact: {{ $host }}
    route:                                                                                                                                                                                    
      - destination:                                                                                                                                                                            
          host: {{ template "cf-broadcaster.fqdn" $ }}
          subset: {{ $host | replace "." "-" -}}
      {{ end -}}
    {{ end -}}
  {{ end -}}
{{ end }}
  - route:
    - destination:
        host: {{ template "cf-broadcaster.fqdn" $ }}
        port:
          number: 80
        subset: base
{{ end }}
