apiVersion: v1
kind: Secret
metadata:
  name: {{ template "charts-manager.fullname" . }}
  labels:
    app: {{ template "charts-manager.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
stringData:
  mongo-uri: {{ coalesce .Values.mongoURI (include "calculateMongoURI" (dict "dbName" .Values.global.chartsManagerService "mongoURI" .Values.global.mongoURI)) }}