{{- if $.Values.global.istio.enabled }}
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ template "pipeline.manager.fullname" $ }}
spec:
  hosts: 
  - {{ template "pipeline.manager.fqdn" $ }}
  http:
{{- range $key,$value := .Values.global.istio.extraDeployments -}}
  {{- range $host, $apps := $value -}}
    {{- range $name, $a := $apps -}}
      {{- if (eq $name "pipeline-manager") }}
  - match:
    - headers:
        x-codefresh-version:
          exact: {{ $host }}
    route:                                                                                                                                                                                    
      - destination:                                                                                                                                                                            
          host: {{ template "pipeline.manager.fqdn" $ }}
          port:
            number: {{ .Values.port }}
          subset: {{ $host | replace "." "-" -}}
      {{ end -}}
    {{ end -}}
  {{ end -}}
{{ end }}
  - route:
    - destination:
        host: {{ template "pipeline.manager.fqdn" $ }}
        port:
          number: {{ .Values.port }}
        subset: base
{{ end }}