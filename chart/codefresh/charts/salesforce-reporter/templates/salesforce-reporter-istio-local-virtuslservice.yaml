{{- if $.Values.global.istio.enabled }}
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ template "salesforce-reporter.fullname" $ }}
spec:
  hosts: 
  - {{ template "salesforce-reporter.fqdn" $ }}
  http:
{{- range $key,$value := .Values.global.istio.extraDeployments -}}
  {{- range $host, $apps := $value -}}
    {{- range $name, $a := $apps -}}
      {{- if (eq $name "salesforce-reporter") }}
  - match:
    - headers:
        x-codefresh-version:
          exact: {{ $host }}
    route:                                                                                                                                                                                    
      - destination:                                                                                                                                                                            
          host: {{ template "salesforce-reporter.fqdn" $ }}
          port:
            number: {{ $.Values.port }}
          subset: {{ $host | replace "." "-" -}}
      {{ end -}}
    {{ end -}}
  {{ end -}}
{{ end }}
  - route:
    - destination:
        host: {{ template "salesforce-reporter.fqdn" $ }}
        port:
          number: {{ $.Values.port }}
        subset: base
{{ end }}